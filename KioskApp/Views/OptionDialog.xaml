<Window x:Class="KioskApp.Views.OptionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"    
        xmlns:local="clr-namespace:KioskApp.Converters"
        Title="옵션 선택" Height="700" Width="700" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    
    <StackPanel Margin="20">
        <!-- 메뉴 정보 : 사진+이름+설명 -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,18" VerticalAlignment="Center">
            <Border Width="85" Height="85" Margin="0,0,16,0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="9">
                <Image Source="{Binding Menu.ImagePath}" Stretch="Uniform" />
            </Border>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="{Binding Menu.Name}" FontWeight="Bold" FontSize="22" />
                <TextBlock Text="{Binding Menu.Description}" Foreground="Gray" FontSize="14" Margin="0,6,0,0"/>
            </StackPanel>
        </StackPanel>

        <!-- 옵션 목록 (동적으로) -->
        <ScrollViewer Height="350" VerticalScrollBarVisibility="Auto" Margin="0,0,0,8">
            <ItemsControl ItemsSource="{Binding MenuOptions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,12">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="{Binding OptionName}" FontWeight="SemiBold"/>
                                <TextBlock Text=" 필수" Foreground="Red" FontWeight="Bold" FontSize="13"
                                           Margin="6,0,0,0"
                                           Visibility="{Binding IsRequired, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </StackPanel>
                            <ComboBox ItemsSource="{Binding Values}"
                                      SelectedItem="{Binding SelectedValue, Mode=TwoWay}"
                                      DisplayMemberPath="ValueLabel" Margin="0,4,0,0"
                                      SelectionChanged="OptionComboBox_SelectionChanged"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- 수량 -->
        <StackPanel Orientation="Horizontal" Margin="0,16,0,0" VerticalAlignment="Center">
            <TextBlock Text="수량" FontWeight="Bold" FontSize="16" Margin="0,0,10,0"/>
            <Button Content="-" Width="30" Height="30" Click="DecreaseQty"/>
            <TextBlock Text="{Binding Quantity}" Width="36" FontSize="18" TextAlignment="Center" VerticalAlignment="Center"/>
            <Button Content="+" Width="30" Height="30" Click="IncreaseQty"/>
        </StackPanel>

        <!-- 합계 -->
        <TextBlock Text="{Binding TotalPriceText}" FontSize="18" FontWeight="Bold" Foreground="DarkRed" Margin="0,16,0,0"/>

        <!-- 담기 버튼 -->
        <Button Content="장바구니 담기" Height="44" Margin="0,24,0,0" Background="#ee3a22" Foreground="White" FontSize="16" Click="AddToCart"/>
    </StackPanel>
</Window>
